# Calendar Editor Changes

- Simplified `js/schedule-data.js` and added clear weekly editing instructions.
- Added `schedule-editor.html`, a visual Monday-Friday schedule generator.
- Added `calendar.html` as a shortcut to the editor.
- Added `EDIT-WEEKLY-SCHEDULE.md` with weekly instructions.
- Added a GitHub issue form for planning a weekly schedule.
- Added a direct-edit shortcut document and backup schedule template.
- Existing `site.js` rendering remains unchanged, so the homepage and class pages continue to use the same schedule data structure.
